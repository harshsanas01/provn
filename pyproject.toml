[project]
name = "api-rate-limiter-poc"
version = "0.1.0"
description = "FastAPI in-memory token-bucket rate limiter"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.7.0",
  "pyyaml>=6.0.0",
  "pytest>=8.0.0",
  "httpx>=0.27.0",
]
